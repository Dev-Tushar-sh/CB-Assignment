#include<iostream>
using namespace std;

int ntriangle(int n)
{
	//base case
	if(n==1)
	return 1;

	//recursive case
	return n+ntriangle(n-1);

}
int main() {

	int n;
	cin>>n;

	cout<<ntriangle(n);


	return 0;
}
